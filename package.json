{
  "name": "reelkeeper",
  "version": "1.0.0",
  "description": "A Docker-hostable SMD component inventory manager with BOM checks and machine API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "yauzl": "^3.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
